# 开发环境Dockerfile
FROM node:16-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install

# 复制源代码
COPY . .

# 创建必要的目录
RUN mkdir -p logs uploads

# 设置环境变量
ENV NODE_ENV=development
ENV PORT=3000

# 暴露端口
EXPOSE 3000

# 运行开发服务器
CMD ["npm", "run", "dev"]